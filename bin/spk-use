#!/usr/bin/env node

const use = require('../lib/use').default
const program = require('commander')

program
  .option('-l, --list', 'List the template\'s contents')
  .option('-s, --safe', 'Don\'t overwrite or delete files')
  .parse(process.argv)

use(program.args[0], program)
